@isTest
public with sharing class sobjectPartnerSoapAPITest {

    @IsTest
    private static void coverSobjectPartnerSoapAPI() {
        Test.startTest();
        new sobjectPartnerSoapAPI.sObject_x();
        Test.stopTest();
    }
}